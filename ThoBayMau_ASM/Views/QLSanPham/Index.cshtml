@model IEnumerable<ThoBayMau_ASM.Models.SanPham>

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var pagingmodel = new ThoBayMau_ASM.Helpers.PagingModel()
            {
                currentpage = (int)(@ViewBag.CurrentPage),
                countpages = (int)(@ViewBag.CountPages),
                generateUrl = (int? p) => Url.Page("", new { p = p, Key = @ViewBag.Search })
            };
}
<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
     aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="container mt-1">
                <div class="cancel-dndk d-flex flex-row-reverse">
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>

                <!-- Tab panes -->
                <div class="tab-content px-5 pb-3">
                    <div class="container tab-pane active">
                        <form name="frm_Delete" asp-action="Delete" method="POST">
                            <div class="mb-3">
                                <input id="txt_ID" class="visually-hidden" type="text" name="txt_ID" />
                            </div>
                            <div class="mb-3 text-center">
                                <h5>Bạn muốn xóa tài khoản này không?</h5>
                            </div>
                            <div class="d-flex justify-content-evenly text-center">
                                <button type="submit" class="btn btn-primary" style="width:100px;">
                                    Có
                                </button>
                                <button type="button" class="btn btn-primary" data-bs-dismiss="modal" aria-label="Close" style="width:100px;">
                                    Không
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="container p-3">
    <div class="row">
        <div class="col-12">
            <h3>Quản lý sản phẩm</h3>
        </div>
    </div>
    <div class="row">
        <div class="col-6">
            <form class="d-flex" role="search" asp-action="Search">
                <input style="max-width:50%;" name="Key" value="@ViewBag.Search" class="form-control me-2" type="search" placeholder="Nhập từ khóa tìm kiếm" aria-label="Tìm kiếm">
                <button class="btn btn-outline-success" type="submit">Tìm kiếm</button>
            </form>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="QLSanPham" asp-action="Create" class="btn btn-primary"><i class="bi bi-plus"></i>Thêm mới</a>
        </div>
    </div>
    <br /><br />
    <div class="table-responsive">
        <table class="table align-middle table-condensed table-hover text-center">
            <thead>
            <th>ID</th>
            <th>Tên</th>
            <th>Hình ảnh</th>
            <th>Mô tả</th>
            <th>Trạng thái</th>
            <th>Mã loại sản phẩm</th>
            <th>Sửa</th>
            <th>Xóa</th>
            <th></th>
            <th>Chi tiết</th>
            </thead>
            <tbody>
                @if (Model != null)
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Ten</td>
                            <td></td>
                            <td>@item.Mota</td>
                            <td>@item.TrangThai</td>
                            <td>@item.LoaiSPId</td>
                            <td class="">

                                <a asp-action="Edit" asp-route-id=@item.Id class="btn btn-primary" style=""><i class="bi bi-pencil-square"></i></a>
                            </td>
                            <td>
                                <button type="button" class="btn btn-red" data-bs-toggle="modal" data-bs-target="#staticBackdrop" data-id="@item.Id" onclick="setId(this)">
                                    <i class="bi bi-trash3"></i>
                                </button>
                            </td>
                            <td>
                                <a asp-route-id=@item.Id asp-action="Index" asp-controller="ChiTietSP" class="btn btn-primary" style=""><i class="bi bi-pencil-square"></i></a>
                            </td>
                            <td>
                                <a data-bs-toggle="collapse" href="#@item.Id" role="button" aria-expanded="false"
                                   aria-controls="@item.Id" style="font-size: 30px"><i class="bi bi-plus-circle"></i></a>
                            </td>
                        </tr>
                        <tr class="collapse" id="@item.Id">

                            <td colspan="8">
                                <p style="text-align:center"><strong>Ảnh sản phẩm</strong></p>
                            </td>
                        </tr>
                    }
                }


            </tbody>
        </table>
    </div>

</div>
<partial name="_Paging" model="pagingmodel" />